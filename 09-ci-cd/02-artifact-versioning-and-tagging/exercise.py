"""
Lesson 02 (Module 09): Exercise — Patch Version Bumper

Task:
Write a function `bump_patch_version(current_tag: str) -> str`:
1. Parses standard tags like `"v1.2.3"` or `"0.9.1"`.
2. Validates format using regular expressions.
3. Increments the patch integer by 1.
4. Preserves the optional `"v"` prefix if present in the input.
5. If invalid tag format is provided, raise a `ValueError`.
"""

# TODO: Implement bump_patch_version function

if __name__ == "__main__":
    pass
