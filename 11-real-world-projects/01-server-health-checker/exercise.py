"""
Project 01: Exercise — JSON-Enabled Health Reporter

Task:
Modify the health checker to support a `--json` CLI argument with `argparse`.
When `--json` is passed:
- Return a JSON formatted string of the metrics dictionary instead of the ASCII table.
- Include `"hostname"`, `"os"`, `"cpu_pct"`, `"mem_pct"`, `"disk_pct"`, and `"status"`.
"""
import argparse
import json

# TODO: Implement JSON-enabled health reporter

if __name__ == "__main__":
    pass
