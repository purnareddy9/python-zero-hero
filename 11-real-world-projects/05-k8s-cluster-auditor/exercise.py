"""
Project 05: Exercise — Cluster Compliance Grade Calculator

Task:
Write a function `calculate_compliance_grade(total_resources: int, violations: int) -> tuple`:
1. If `total_resources <= 0`, return `(100.0, "A")`.
2. Compute `score = round(((total_resources - violations) / total_resources) * 100, 1)`.
3. If score >= 90: Grade "A"
   If score >= 80: Grade "B"
   If score >= 70: Grade "C"
   Otherwise: Grade "F"
4. Return `(score, grade)`.
"""

# TODO: Implement calculate_compliance_grade function

if __name__ == "__main__":
    pass
